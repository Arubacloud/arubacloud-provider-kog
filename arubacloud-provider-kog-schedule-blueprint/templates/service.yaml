apiVersion: v1
kind: Service
metadata:
  name: {{ include "schedule-plugin-chart.fullname" . }}
  labels:
    {{- include "schedule-plugin-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "schedule-plugin-chart.selectorLabels" . | nindent 4 }}

