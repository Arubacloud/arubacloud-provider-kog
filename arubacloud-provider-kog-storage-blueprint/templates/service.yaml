apiVersion: v1
kind: Service
metadata:
  name: {{ include "storage-plugin-chart.fullname" . }}
  labels:
    {{- include "storage-plugin-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "storage-plugin-chart.selectorLabels" . | nindent 4 }}

