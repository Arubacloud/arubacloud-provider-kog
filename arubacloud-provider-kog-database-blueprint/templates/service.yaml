apiVersion: v1
kind: Service
metadata:
  name: {{ include "database-plugin-chart.fullname" . }}
  labels:
    {{- include "database-plugin-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "database-plugin-chart.selectorLabels" . | nindent 4 }}

