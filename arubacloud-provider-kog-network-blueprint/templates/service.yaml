apiVersion: v1
kind: Service
metadata:
  name: {{ include "network-plugin-chart.fullname" . }}
  labels:
    {{- include "network-plugin-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "network-plugin-chart.selectorLabels" . | nindent 4 }}

